#!/usr/bin/env python

from apps.katello.locators import KatelloLocators
from selenium.webdriver.common.by import By

class SAMLocators(KatelloLocators):
    """
    Locators for katello pages are contained within.
    """
